function contract(){let s=state.settings,notes=noteDates(),paidNotes=notes.filter(d=>rentStatus(d.slice(0,7))==='paid').length;document.getElementById('contract').innerHTML=`<h1 class="title">Contrato</h1><div class="muted" style="margin-bottom:14px">Información privada guardada en tu dispositivo</div><div class="card"><div class="row"><div><b>Contrato de arrendamiento</b><div class="small muted">${s.contractSigned?'Firmado '+fmt(s.contractSigned):'Fecha de firma pendiente'}</div></div><span class="badge ${s.contractStart?'ok':'warn'}">${s.contractStart?'Configurado':'Configurar'}</span></div><div class="kv"><span class="muted">Vigencia</span><b>${fmt(s.contractStart)} — ${fmt(s.contractEnd)}</b></div><div class="kv"><span class="muted">Inquilino</span><b>${esc(s.tenantName||'—')}</b></div><div class="kv"><span class="muted">Contacto</span><b>${esc(s.tenantPhone||'—')}<br><span class="small">${esc(s.tenantEmail||'')}</span></b></div></div><div class="card"><div class="kv"><span class="muted">Mensualidad total</span><b>${s.rentAmount?money(s.rentAmount):'Sin configurar'}</b></div><div class="kv"><span class="muted">Interés moratorio</span><b>${s.moratoryInterestPercent?esc(s.moratoryInterestPercent)+'% mensual':'Sin configurar'}</b></div><div class="kv"><span class="muted">Periodo de gracia</span><b>${Number(s.rentGraceDays||0)} días naturales</b></div><div class="kv"><span class="muted">Mantto</span><b>${s.maintenanceAmount?money(s.maintenanceAmount):'Sin configurar'}</b></div><div class="kv"><span class="muted">Casa Club</span><b>${s.clubAmount?money(s.clubAmount):'Sin configurar'}</b></div><div class="small muted">RenT separa los servicios para que puedas controlar su pago, aunque formen parte de la mensualidad acordada.</div></div><div class="card"><div class="kv"><span class="muted">Primer pago</span><b>${s.firstPaymentAmount?money(s.firstPaymentAmount):'Sin configurar'} · ${firstPaymentPaid()?'Pagado':'Pendiente'}</b></div><div class="small muted">Primera renta + depósito. No requiere pagaré.</div><div class="kv"><span class="muted">Depósito</span><b>${s.depositAmount?money(s.depositAmount):'Sin configurar'} · ${s.depositPaid?'Pagado':'Pendiente'}</b></div></div><div class="card"><div class="row"><div><b>${s.promissoryCount} pagarés</b><div class="small muted">Mensualidades posteriores a la primera</div></div><span class="badge info">${paidNotes}/${s.promissoryCount} pagados</span></div>${notes.length?notes.map((d,i)=>`<div class="kv"><span>Pagaré ${i+1}/${s.promissoryCount} · ${fmt(d)}</span><b class="${rentStatus(d.slice(0,7))==='paid'?'':'muted'}">${rentStatus(d.slice(0,7))==='paid'?'✓ Pagado':money(s.rentAmount)}</b></div>`).join(''):'<div class="small muted" style="padding-top:12px">Configura la fecha de inicio para generar el calendario.</div>'}</div><div class="card"><div class="kv"><span class="muted">Cuota administrativa</span><b>${s.adminFeeAmount?money(s.adminFeeAmount):'Sin monto'} · ${s.adminFeePaid?'Pagada':'Pendiente'}</b></div><div class="small muted">${esc(s.adminFeeNote||'')}</div></div><button id="cedit" class="btn primary full">Editar datos</button><button id="cdoc" class="btn secondary full" style="margin-top:8px">Ver documentos</button>`;cedit.onclick=contractModal;cdoc.onclick=()=>{docFilter='contract';navigate('documents')}}
function contractModal(){let s=state.settings;modal('Datos del contrato',`<form id="cf"><div class="field"><label>Inquilino</label><input id="tn" class="input" value="${esc(s.tenantName)}"></div><div class="grid"><div class="field"><label>Email</label><input id="te" class="input" type="email" value="${esc(s.tenantEmail)}"></div><div class="field"><label>Teléfono</label><input id="tp" class="input" value="${esc(s.tenantPhone)}"></div></div><div class="grid"><div class="field"><label>Firma</label><input id="csg" class="input" type="date" value="${s.contractSigned}"></div><div class="field"><label>Primer pago</label><input id="fpd" class="input" type="date" value="${s.firstPaymentDue}"></div></div><div class="grid"><div class="field"><label>Inicio</label><input id="cs" class="input" type="date" value="${s.contractStart}"></div><div class="field"><label>Fin</label><input id="ce" class="input" type="date" value="${s.contractEnd}"></div></div><div class="grid"><div class="field"><label>Interés moratorio % mensual</label><input id="mi" class="input" type="number" min="0" max="100" step="0.01" value="${s.moratoryInterestPercent||''}"></div><div class="field"><label>Número de pagarés</label><input id="pc" class="input" type="number" min="0" max="36" value="${s.promissoryCount}"></div></div><div class="grid"><div class="field"><label>Depósito MXN</label><input id="da" class="input" type="number" value="${s.depositAmount||''}"></div><div class="field"><label>Fecha depósito</label><input id="dd" class="input" type="date" value="${s.depositDate}"></div></div><label><input id="dp" type="checkbox" ${s.depositPaid?'checked':''}> Depósito pagado</label><div class="grid" style="margin-top:12px"><div class="field"><label>Cuota admin MXN</label><input id="aa" class="input" type="number" value="${s.adminFeeAmount||''}"></div><div class="field"><label>Fecha pagada</label><input id="ad" class="input" type="date" value="${s.adminFeeDate}"></div></div><div class="field"><label>Nota admin</label><input id="an" class="input" value="${esc(s.adminFeeNote)}"></div><label><input id="ap" type="checkbox" ${s.adminFeePaid?'checked':''}> Cuota pagada</label><button class="btn primary full" style="margin-top:14px">Guardar</button></form>`);cf.onsubmit=e=>{e.preventDefault();Object.assign(s,{tenantName:tn.value,tenantEmail:te.value,tenantPhone:tp.value,contractSigned:csg.value,firstPaymentDue:fpd.value,contractStart:cs.value,contractEnd:ce.value,moratoryInterestPercent:Number(mi.value||0),depositAmount:Number(da.value||0),promissoryCount:Number(pc.value||0),depositDate:dd.value,depositPaid:dp.checked,adminFeeAmount:Number(aa.value||0),adminFeeDate:ad.value,adminFeeNote:an.value,adminFeePaid:ap.checked});s.firstPaymentAmount=Number(s.rentAmount)+Number(s.depositAmount);save();closeModal();contract();toast('Contrato actualizado')}}
function db(){return new Promise((res,rej)=>{let r=indexedDB.open('rent-docs',1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('docs'))r.result.createObjectStore('docs',{keyPath:'id'})};r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}async function docsAll(){let d=await db();return new Promise((res,rej)=>{let r=d.transaction('docs').objectStore('docs').getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}async function docPut(o){let d=await db();return new Promise((res,rej)=>{let t=d.transaction('docs','readwrite');t.objectStore('docs').put(o);t.oncomplete=res;t.onerror=()=>rej(t.error)})}async function docDel(id){let d=await db();return new Promise((res,rej)=>{let t=d.transaction('docs','readwrite');t.objectStore('docs').delete(id);t.oncomplete=res;t.onerror=()=>rej(t.error)})}
async function documents(){let all=await docsAll(),shown=docFilter==='all'?all:all.filter(d=>d.cat===docFilter);document.getElementById('documents').innerHTML=`<h1 class="title">Documentos</h1><div class="muted" style="margin-bottom:14px">Contrato, comprobantes e identificaciones</div><div class="alert info"><b>Privado en tu dispositivo</b><div class="small">Los documentos que subas aquí se guardan localmente y no se publican en GitHub.</div></div><button id="up" class="upload"><b>＋ Subir documento</b><div class="small muted">PDF, JPG o PNG</div></button><div class="tabs"><button class="tab ${docFilter==='all'?'active':''}" data-f="all">Todos</button>${Object.entries(CATS).map(([k,v])=>`<button class="tab ${docFilter===k?'active':''}" data-f="${k}">${v}</button>`).join('')}</div><div class="card">${shown.length?shown.map(d=>`<div class="doc"><div class="ico">${d.type.includes('pdf')?'PDF':'IMG'}</div><div data-open="${d.id}" style="cursor:pointer"><b>${esc(d.title)}</b><div class="small muted">${CATS[d.cat]} · ${Math.round(d.size/1024)} KB</div></div><button class="close" data-del="${d.id}">×</button></div>`).join(''):'<div class="muted" style="text-align:center;padding:20px">Sin documentos</div>'}</div>`;up.onclick=docModal;document.querySelectorAll('[data-f]').forEach(b=>b.onclick=()=>{docFilter=b.dataset.f;documents()});document.querySelectorAll('[data-open]').forEach(x=>x.onclick=()=>{let d=all.find(a=>a.id===x.dataset.open);window.open(d.data,'_blank')});document.querySelectorAll('[data-del]').forEach(x=>x.onclick=async()=>{await docDel(x.dataset.del);documents();toast('Documento eliminado')})}
function docModal(){modal('Subir documento',`<form id="df"><div class="field"><label>Archivo</label><input id="file" class="input" type="file" accept="application/pdf,image/*" required></div><div class="field"><label>Título</label><input id="dt" class="input" placeholder="Ej. Contrato firmado"></div><div class="field"><label>Categoría</label><select id="dc" class="input">${Object.entries(CATS).map(([k,v])=>`<option value="${k}" ${k===docFilter?'selected':''}>${v}</option>`).join('')}</select></div><button class="btn primary full">Guardar</button></form>`);df.onsubmit=e=>{e.preventDefault();let f=file.files[0],r=new FileReader();r.onload=async()=>{await docPut({id:crypto.randomUUID(),title:dt.value||f.name,cat:dc.value,name:f.name,type:f.type,size:f.size,data:r.result});closeModal();documents();toast('Documento guardado')};r.readAsDataURL(f)}}
function more(){let s=state.settings;document.getElementById('more').innerHTML=`<h1 class="title">Más</h1><div class="muted" style="margin-bottom:14px">Configuración y respaldo</div><div class="card"><div class="kv"><span>Propiedad</span><b>${esc(s.propertyName)}</b></div><div class="kv"><span>Renta mensual</span><b>${s.rentAmount?money(s.rentAmount):'Sin configurar'}</b></div><div class="kv"><span>Primer pago</span><b>${s.firstPaymentAmount?money(s.firstPaymentAmount):'Sin configurar'}</b></div><div class="kv"><span>Mantto</span><b>${s.maintenanceAmount?money(s.maintenanceAmount):'Sin configurar'}</b></div><div class="kv"><span>Casa Club</span><b>${s.clubAmount?money(s.clubAmount):'Sin configurar'}</b></div><button id="set" class="btn primary full" style="margin-top:12px">Editar configuración</button></div><div class="card"><b>Respaldo / configuración privada</b><p class="small muted">Puedes importar un archivo de configuración privado sin borrar tus pagos existentes.</p><button id="exp" class="btn secondary full">Exportar respaldo</button><label class="btn secondary full" style="display:block;text-align:center;margin-top:8px">Importar respaldo o configuración<input id="imp" type="file" accept="application/json" hidden></label></div><div class="card about"><img src="assets/logo.svg" alt="RenT"><div class="small muted">RenT v1.1 · datos privados guardados localmente</div></div>`;set.onclick=settingsModal;exp.onclick=exportBackup;imp.onchange=importBackup}
function settingsModal(){let s=state.settings;modal('Configuración',`<form id="sf"><div class="field"><label>Propiedad</label><input id="sp" class="input" value="${esc(s.propertyName)}"></div><div class="field"><label>Ubicación</label><input id="sl" class="input" value="${esc(s.propertyLocation)}"></div><div class="grid"><div class="field"><label>Renta mensual MXN</label><input id="sr" class="input" type="number" value="${s.rentAmount||''}"></div><div class="field"><label>Día de pago renta</label><input id="srd" class="input" type="number" min="1" max="28" value="${s.rentDueDay}"></div></div><div class="field"><label>Días naturales de gracia</label><input id="srg" class="input" type="number" min="0" max="15" value="${s.rentGraceDays}"></div><div class="grid"><div class="field"><label>Mantto mensual</label><input id="sm" class="input" type="number" value="${s.maintenanceAmount||''}"></div><div class="field"><label>Casa Club mensual</label><input id="sc" class="input" type="number" value="${s.clubAmount||''}"></div></div><div class="field"><label>Día vencimiento servicios (opcional)</label><input id="ssd" class="input" type="number" min="1" max="28" placeholder="Sin configurar" value="${s.serviceDueDay||''}"></div><button class="btn primary full">Guardar</button></form>`);sf.onsubmit=e=>{e.preventDefault();Object.assign(s,{propertyName:sp.value,propertyLocation:sl.value,rentAmount:Number(sr.value||0),rentDueDay:Number(srd.value||15),rentGraceDays:Number(srg.value||0),maintenanceAmount:Number(sm.value||0),clubAmount:Number(sc.value||0),serviceDueDay:Number(ssd.value||0)});s.firstPaymentAmount=Number(s.rentAmount)+Number(s.depositAmount);save();closeModal();more();toast('Configuración guardada')}}
async function exportBackup(){let docs=await docsAll(),blob=new Blob([JSON.stringify({app:'RenT',version:APP_VERSION,state,docs})],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='RenT-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}async function importBackup(e){let f=e.target.files[0];if(!f)return;try{let p=JSON.parse(await f.text());if(p.app==='RenTConfig'){state.settings={...state.settings,...(p.settings||{})};state.notes={...state.notes,...(p.notes||{})};state.selectedMonth=p.selectedMonth||state.selectedMonth;state.schemaVersion=APP_VERSION;save();more();toast('Configuración privada importada')}else if(p.app==='RenT'){state=p.state;state.schemaVersion=APP_VERSION;save();for(let d of p.docs||[])await docPut(d);more();toast('Respaldo importado')}else throw Error('Archivo inválido')}catch(x){alert(x.message)}e.target.value=''}
document.querySelectorAll('.nav').forEach(n=>n.onclick=()=>navigate(n.dataset.view));window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;install.style.display='block'});install.onclick=async()=>{if(deferredPrompt){deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;install.style.display='none'}};if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'));save();home();